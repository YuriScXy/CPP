#include <bits/stdc++.h>

using namespace std;

	int main()
	{
		
		int n;

		cin >> n;

		double n1;

		for (int i = 0; i < n; ++i)
		{
			cin >> n1;

			cout << fixed << showpoint << setprecision(4) << sqrt(n1) << endl;			

		}

		return 0;
	}